interface:
  display_name: "SCIR Tier Profile Auditor"
  short_description: "Audit SCIR tiers, profiles, and claims"
  default_prompt: "Use $scir-tier-profile-auditor to audit SCIR tiers, profiles, preservation labels, and boundary-accounting claims before validating a change."
policy:
  allow_implicit_invocation: true
