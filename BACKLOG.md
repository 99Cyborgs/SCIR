# BACKLOG
Status: Ranked

## Frozen support work

1. Keep `SCIR-H -> SCIR-L` invariant enforcement strict against the frozen 11-case Python executable proof loop.
2. Keep Track `A`, Track `B`, and the retained non-default Track `C` pilot synchronized to the frozen executable proof-loop case set, but subordinate to importer and validator value.
3. Keep helper-free Wasm emitted-case lists, lowering-rule boundaries, and fixed record-cell ABI wording synchronized to the frozen retained backend slice.

## Next decisions

1. Keep broader object and exception semantics beyond the exact admitted class and `d_try_except` slices rejected or deferred until new canonical fixtures and decision-register updates exist.
2. Treat any future Rust round-trip, backend, or benchmark activation as a post-MVP reactivation decision, not the next automatic implementation phase.
3. Treat any future helper-free Wasm widening, backend activation, or benchmark promotion as a post-MVP reactivation decision, not the next automatic implementation phase.
4. Treat any future Track `C` promotion into the default executable gate or a broader benchmark claim surface as a post-MVP reactivation decision, not the next automatic implementation phase.

## Deferred

1. Any grammar widening.
2. Any new frontend beyond the bounded Python and Rust surfaces already on disk.
3. Any release-bundle expansion beyond explicit opt-in tooling.
4. Any native-parity, `D-JS`, or broad runtime/tooling claims.
