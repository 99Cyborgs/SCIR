# BACKLOG
Status: Ranked

## Frozen support work

1. Harden `SCIR-H -> SCIR-L` invariant enforcement without widening the admitted lowering subset.
2. Keep Rust importer evidence aligned to canonical `SCIR-H` without promoting Rust into the active proof loop.
3. Keep the bounded Wasm reference backend explicit and subset-bound; no widening without a spec-first contract decision.
4. Keep Track `A` and Track `B` benchmark doctrine runnable, but subordinate to importer and validator value.

## Deferred

1. Any grammar widening.
2. Any new frontend beyond the bounded Python and Rust surfaces already on disk.
3. Any release-bundle expansion beyond explicit opt-in tooling.
4. Any native-parity, `D-JS`, or broad runtime/tooling claims.
