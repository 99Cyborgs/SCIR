# TypeScript Import Scope
Status: Normative

## State

This subtree is deferred and not part of the active SCIR MVP.

The checked-in placeholder material remains archived historical residue only.
Historical case ids are retained for traceability, not as live tier claims.

## Rule

Nothing under `frontend/typescript/` may be treated as an active importer, lowering, reconstruction, benchmark, or host-runtime commitment until the root boundary files move it back into scope.
Archived placeholder bundles under `tests/typescript_importer/` remain `Tier D` residue and must not carry canonical `SCIR-H` or active-profile implications.
