# NOT ACTIVE

The default script surface excludes deferred importers and speculative tooling helpers.

- `scripts/typescript_importer_conformance.py` is retained on disk only as a deferred surface.
- It is not part of the default validation or benchmark gate.
