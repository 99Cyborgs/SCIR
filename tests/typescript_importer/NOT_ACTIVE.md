# NOT ACTIVE

`tests/typescript_importer` is an archived placeholder corpus.

- It is not part of the default validation or benchmark gate.
- The checked-in cases remain historical residue only until an explicit reactivation decision promotes them.
